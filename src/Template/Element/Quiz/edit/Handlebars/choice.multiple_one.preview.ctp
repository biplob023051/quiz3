<div class="row">
    <div class="col-xs-12 col-md-4">
        <div class="radio">
            <label>
                <input type="radio" disabled value="{{id}}" name="questions[{{question_id}}][correct_choice]" />
                {{text}}
            </label>
        </div>
    </div>
</div>